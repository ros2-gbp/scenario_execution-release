# Scenario Execution Library for data operations

The `scenario_execution_dataops` package provides actions to modify data

It provides the following scenario execution library:

- `dataops.osc`: Actions for data operations

